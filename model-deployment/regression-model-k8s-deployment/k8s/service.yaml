apiVersion: v1
kind: Service
metadata:
  name: regression-model-service
  namespace: default
spec:
  type: NodePort
  selector:
    app: regression-model
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30080